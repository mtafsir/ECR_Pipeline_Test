name: ECS Workflow
on: 
  push:
    branches: 
      - ECR_Pipeline_Test
      - main
    paths:
      - 'ECR_Container_Test/**'
  workflow_dispatch:
  
jobs:
  ECR-Second:
    runs-on: unbuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
        with:
          repository: mtafsir/ECR_Pipeline_Test
          token: ${{ secrets.PAT }}
          ref: main
      - name : test 
        run: ls
