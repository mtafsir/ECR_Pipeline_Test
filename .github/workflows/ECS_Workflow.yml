name: ECS Workflow
on: 
  push:
    branches: [New_ECS]
  workflow_dispatch:
  
jobs:
  ECR-Deployment:
    runs-on: unbuntu-latest
    steps:
      - name: print greeting
        run: echo "Hello World"
      - name: print goodbye
        run : echo "Good Bye!" 

