name: ECS Workflow
on: 
  push:
    branches: [ECR_Pipeline_Test]
  workflow_dispatch:
  
jobs:
  ECR-Deployment:
    runs-on: unbuntu-latest
    steps:
      - name: print greeting
        run: echo "Hello World"
      - name: print goodbye
        run : echo "Good Bye!" 
